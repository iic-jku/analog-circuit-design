::: {.content-hidden}
Copyright (C) 2024-2026 Harald Pretl and co-authors (harald.pretl@jku.at)

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
:::

```{python}
#| label: fig-basic-ota-small-signal-noise
#| echo: false
#| fig-cap: "5-transistor OTA small-signal model for noise calculation (added noise sources shown in blue)."
import schemdraw as sd
import schemdraw.elements as elm
sd.svgconfig.svg2 = False
with sd.Drawing(canvas='svg') as d:
    d.config(unit=2)
    d.config(fontsize=16)
    
    d.push()
    elm.Line().left().length(1)
    elm.Line().down(2)
    elm.Ground()
    d.pop()
    elm.Gap().down().label(['+', r'$V_\mathrm{gs1}$', '-'])
    elm.Line().right().length(3).dot()

    d.push()
    Ids1 = elm.SourceI().label(r'$g_\mathrm{m1,2} V_\mathrm{gs1}$').reverse()
    d.push()
    elm.Line().right(2).idot().color('blue')
    In1 = elm.SourceI().down().label(r'$\overline{I_\mathrm{n1}^2}$').color('blue')
    elm.Ground().color('blue')
    d.pop()
    elm.Line().length(2).dot()
    d.push()
    gm34 = elm.Resistor().up().label(r'$g_\mathrm{m3,4}$')
    elm.Vdd()
    d.pop()
    elm.Line().right().length(2).color('blue')
    In3 = elm.SourceI().up().label(r'$\overline{I_\mathrm{n3}^2}$').color('blue')
    elm.Vdd().color('blue')
    d.pop()
    elm.Ground()

    d.move(dx=7)
    elm.Ground()
    Ids2 = elm.SourceI().label(r'$g_\mathrm{m1,2} V_\mathrm{gs2}$').reverse()
    d.push()
    elm.Line().left(3).idot().color('blue')
    In2 = elm.SourceI().down().label(r'$\overline{I_\mathrm{n2}^2}$').color('blue')
    elm.Ground().color('blue')
    d.pop()
    elm.Line().up().length(1).dot()
    out = d.here
    elm.Line().up().length(1)
    Ids4 = elm.SourceI().label(r'$g_\mathrm{m3,4} V_\mathrm{gs3}$')
    d.push()
    elm.Line().at(Ids4.start).left(3).idot().color('blue')
    In4 = elm.SourceI().up().label(r'$\overline{I_\mathrm{n4}^2}$').color('blue')
    elm.Vdd().color('blue')
    d.pop()
    elm.Vdd()
    
    elm.Line().right().at(out).length(4.5).label(r'$V_\mathrm{out}$').dot()
    d.push()
    elm.Line().down().length(1)
    Cload = elm.Capacitor().down().label(r'$C_\mathrm{load}$')
    elm.Ground()

    d.pop()
    elm.Line().right().length(3)
    elm.Line().down().length(1)
    gds24 = elm.Resistor().down().label(r'$g_\mathrm{ds2} + g_\mathrm{ds4}$')
    elm.Ground()
    elm.Line().right().at(Ids2.start).length(2).idot()
    elm.Gap().up().label(['-', r'$V_\mathrm{gs2}$', '+'])
    elm.Line().up(1).dot()

    elm.Line().left().length(3).at(gm34.start)
    elm.Gap().up().label(['+', r'$V_\mathrm{gs3}$', '-'])
    elm.Vdd()
```
